---
system: osx
os: osx

executables:
  bash: /bin/bash
  brew: /usr/local/bin/brew
  vim: /usr/bin/vim
  zsh: /usr/local/bin/bash

directories:
  etc: /usr/local/etc
  var: /usr/local/var

me:
  user:
    name: richard
    group: staff
    shell: "{{ executables.zsh }}"

  template_home: home/richard
  home: /Users/richard
  system: osx
